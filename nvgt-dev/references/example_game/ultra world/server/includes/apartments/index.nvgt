#include "*.nvgt"
namespace apartments {
	string mapget(string t, string mapname, string owner) {
		//return t.trim_whitespace().replace("%00mapname00%", mapname).replace("%00owner00%", owner);
		return houses::mapget(t, mapname, owner);
	}
}
