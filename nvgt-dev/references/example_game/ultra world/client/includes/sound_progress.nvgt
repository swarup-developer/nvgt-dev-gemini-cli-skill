#include "bgt_compat.nvgt"#include "bgt_compat.nvgt"void progress_pan(sound@ handle, int current, int max) {
handle.pan = (current * 200 / max) - 100;
//handle.stop();
//handle.play();
}
void progress_pitch(sound@ handle, int current, int max) {
	handle.pitch = (current * 200 / max);
	handle.stop();
	handle.play();
}