void main() {
	sound s;
	string f = input_box("Sound", "Sound");
	if (f == "") exit();
	if (!f.ends_with(".ogg")) f += ".ogg";
	f.insert(0, "packs/sounds/");
	if (!s.load(f)) {
		alert("Lol", "Failed");
		exit();
	}
	clipboard_set_text(s.length);
	s.close();
	alert("Copied", "Done");
	exit();
}
