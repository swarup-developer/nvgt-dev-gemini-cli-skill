#include "bgt_compat.nvgt"
sound test;
void main() {
string snd=input_box("fn","Enter the name of the file you would like to check");
if(snd=="")
exit();
test.load("sounds/"+snd+".ogg");
alert("Hello","The file is about "+test.length()+" ms int. If we were to round it, it would be "+round(test.length(),0)+" ms");
clipboard_copy_text(test.length()+"");
exit();
}
